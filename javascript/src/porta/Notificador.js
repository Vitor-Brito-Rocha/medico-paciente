import { naoImplementado } from './_contrato.js';

// OCP: novo canal de notificacao = nova implementacao, zero alteracao no servico.
export class Notificador {
  notificar(usuario, mensagem) {
    naoImplementado(this.constructor.name, 'notificar');
  }
}
