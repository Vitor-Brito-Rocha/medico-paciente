import { naoImplementado } from './_contrato.js';

// Evolucao futura do sistema: triagem assistida por IA.
export class AnalisadorClinico {
  analisar(consulta, documentos) {
    naoImplementado(this.constructor.name, 'analisar');
  }
}
