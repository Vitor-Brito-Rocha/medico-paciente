import { naoImplementado } from './_contrato.js';

export class AutenticacaoService {
  login(email, senha) {
    naoImplementado(this.constructor.name, 'login');
  }
  logout(sessao) {
    naoImplementado(this.constructor.name, 'logout');
  }
}
